@model IEnumerable<IngSoftware.Models.Respuesta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Peticion.Tema)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contenido)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Peticion.Tema)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contenido)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Respuesta }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Respuesta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Respuesta })
        </td>
    </tr>
}

</table>
